// Default content for the `@modal` parallel-route slot when no
// intercepting route matches the current URL. Next.js requires a
// default for named slots so it can render the layout in initial
// navigation states.
export default function ModalDefault() {
  return null;
}
