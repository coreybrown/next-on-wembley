import { LoginCard } from "@/components/login-card";

export default function LoginPage() {
  return (
    <main className="bg-login flex min-h-svh items-center justify-center px-6 py-16">
      <LoginCard />
    </main>
  );
}
